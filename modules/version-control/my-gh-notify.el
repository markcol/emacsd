;;; my-gh-notify.el --- gh-notify configuration. -*- lexical-binding: t; -*-

;;; Commentary:

;; Basic configuration for gh-notify.

;;; Code:

(use-package gh-notify
  :defer t
  :commands gh-notify)

(provide 'my-gh-notify)
;;; my-gh-notify.el ends here
